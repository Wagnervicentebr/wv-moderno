
  # wv versao soft (Copy)

  This is a code bundle for wv versao soft (Copy). The original project is available at https://www.figma.com/design/NZQCiaJ0RpIeOXmPHRcD2h/wv-versao-soft--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  